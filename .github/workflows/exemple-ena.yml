name: Exemple By ENA

on:
  push:
    branches:
      - ena-test
jobs:
  build:
    name: build
    runs-on: ubuntu-18.04
    steps:
      - name: Checkout
        uses: actions/checkout@v2
    
      - name: Build
        run: docker build -t demo .